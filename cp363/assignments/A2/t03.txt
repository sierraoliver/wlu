SELECT member.memberSurname, member.memberForename, pub.pubTitle FROM member 
JOIN pub ON member.memberId = pub.pubMemberId
JOIN pubType ON pub.pubPubType = pubType.pubType
WHERE pubType.pubTypeDesc = 'Conference Paper' AND pub.pubTitle LIKE '%Nuclear%'
ORDER BY pub.pubTitle